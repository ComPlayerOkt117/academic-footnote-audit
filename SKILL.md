---
name: academic-footnote-audit
description: Audit academic-paper footnotes in PDF, DOCX, or paired DOCX/PDF manuscripts against page layout and complete source documents.
metadata:
  short-description: Academic footnote audit orchestrator
---

# Academic Footnote Audit

本技能是“全文脚注核对”的唯一用户入口，采用“总编排器 + 阶段手册 + 固定脚本 + 阶段闸门”模式。PDF和DOCX不是两套流程；它们只在证据采集适配器上不同，进入规范记录后必须走同一条流水线。

## 总路线

```text
最小输入检查 → 证据准备 → 脚注全量盘点 → 引文边界与母文献身份逐条语义定界
→ 规范记录 → 已提供来源的局部核对 → 统一初版输出
→ 交付前模型终检（最多一次修正）→ 最终统一输出 → 输出闸门与报告
```

阶段规则见：

- [workflow.md](references/workflow.md)：阶段、状态、断点续作和停止条件；
- [evidence.md](references/evidence.md)：PDF/DOCX证据层级、OCR和页面证据；
- [boundary.md](references/boundary.md)：脚注标记与引文范围判定；
- [sources.md](references/sources.md)：母文献、来源材料和分批核对；
- [outputs.md](references/outputs.md)：统一记录、HTML/CSV和交付验收；
- [delegation.md](references/delegation.md)：长文档的内部并行处理。

只在当前阶段需要时读取对应手册，不要每次重读全部规则。

## 不可违反的总原则

1. 原始稿件只读保存，不直接修改。
2. 页面问题看页面证据；字符和脚注内容看原生文本/OOXML；OCR只是辅助。
3. 脚注标记通常对应标记之前的文本。严禁从段首复制到当前标记，也严禁把标记之后的文本当作引文。
4. 引文范围必须是有语义边界的候选单元；不能只剩一个引号或几个尾字，也不能无解释地吞入上文。
5. 同一句中多个连续引语可以构成一个“连续共引”单元；不能因机械截断而漏掉前一引语，也不能因共享来源而自动合并无关句子。
6. **引文 span 必须逐条语义判定，禁止编写脚本/正则批量抽取。** 每个标记按各自语境定界（理由随条而异）；不得把标记前的作者论述整段吞入 span。判别信号：若一批 span 的边界理由完全雷同、或普遍超过数十字乃至上百字，即疑似程序化生成，需逐条复核（见 boundary.md）。
   规范记录生成前，模型必须先逐条完成这项判定：以页面中的脚注标记为锚点，结合原生文本和页面图确定完整、有界的引文，再写入 `canonical_records.json`。不得先用自动截取结果填满记录、交给流水线后再把语义判断当作补救。
7. 缺少来源时可以完成盘点和清单，不能声称来源、页码或引文真伪已经核实。
8. 无法裁决的OCR/字形冲突必须标为 `待人工复核`，不得凭直觉强行修复；单条疑难记录不阻塞整份初步清单。
9. 正式交付物（用户可读的HTML/CSV成果）必须从规范记录经固定生成器产生；模型不得手工拼接或冒称正式HTML/CSV。为准备规范记录而写的一次性工具脚本允许放在 `04_工作文件/` 并在交付说明中声明用途，但它只能产出中间记录，不能产出或冒充正式交付物。

10. 交付前由模型查看实际 HTML/CSV 和规范记录，做一次语义终检。重点看引文是否残缺或无限上溯、连续共引是否误拆/误合、母文献题名是否污染、OCR与原生文本是否冲突。发现问题时回看原始证据，只修正规范记录并重渲染一次；仍有疑问就标为 `待人工复核`，不得循环修补。该终检是软约束，不新增脚本、闸门或复杂记录制度。

## 入口行为

收到核对请求后：

1. 只做最小的只读输入识别，确认项目目录内的PDF、DOCX和既有工作区。
2. 只有DOCX时，说明缺少页面证据并暂停实质处理，请用户提供对应PDF，或明确同意无页面证据的风险路线；未得到选择前不得创建工作区和清单。
3. 只有PDF时，询问是否有对应DOCX；没有时仍可继续PDF流程，但记录OOXML证据不可用。
4. 有DOCX+PDF时，确认两者是同一版本；DOCX负责原生字符、真实脚注和结构，PDF负责页面版式。
5. 进入阶段后，以规范记录和状态文件为准恢复，不重复已经通过闸门的阶段。

例行只读操作不打断用户提问。只有会改变证据基础、核对范围、稿件内容或删除内容的决定，才在对应阶段提出一个明确问题。

## 固定执行和完成条件

规范记录完成后，正式输出只能通过唯一入口 `scripts/audit_pipeline.py`。该入口依次调用来源规范化器、规范渲染器、输出检查，并写入流程凭据和违规记录。 

可信度由最终输出闸门保证（渲染器凭据、哈希、表头、行数校验），项目目录内的脚本（含 `04_工作文件/` 中的一次性工具脚本）只记录用途、不因存在而阻断流水线。未经统一渲染器生成的表格、模型手工拼接或临时脚本冒称的成果，只能标为“中间记录/未完成”，不得交付为正式成果。若规范记录缺失、母文献列表缺失或污染、检查失败、生成凭据缺失，只能报告“未完成/中间记录”，不得交付为正式成果。

固定生成器只保存在本技能的 `scripts/` 中；`04_工作文件/` 只允许存放中间记录与一次性工具脚本（用途须声明），正式交付物统一输出到 `03_核对记录/`。

母文献身份识别是盘点阶段模型的语义任务：逐条填写作者/编者、题名/析出、类型、出版信息等字段（作者判定须有责任标记证据，见 [sources.md](references/sources.md)），不依赖 `[M][J][N]` 等仅国标采用的标记，按 [sources.md](references/sources.md) 的转引与析出规则处理。`scripts/normalize_sources.py` 只做归并去重与兜底：优先采用已填字段，空白时才正则提取，无法识别标 `待人工补判` 而非硬猜。正式渲染器不再根据脚注临时重建母文献清单；规范记录必须明确提供 `sources` 列表，否则由来源规范化器生成，仍缺失时直接阻塞。

完成报告必须区分：已完成、待补材料、待人工复核、未完成。记录闸门采用“整份结构阻塞、单条内容复核”原则；单条疑问可生成带复核队列的初步成果并标记 `passed_with_review`，但只有来源核对和最终闸门全部通过、且不存在未处理的实质疑问时，才允许使用“完成全文核对”的表述。

## 交付

最终回复用中文，按“结果摘要—当前限制—下一步—成果导航”组织。凡提及已生成的文件或文件夹，都必须确认存在并使用可点击的绝对路径链接。链接必须严格写成 `[显示名称](<D:/绝对/路径/文件>)`：使用正斜杠、尖括号和绝对路径；禁止输出裸路径、反斜杠路径或未加尖括号的 Windows 路径。文件名必须以 [outputs.md](references/outputs.md) 的规范名称为准，不得自行改名或混用历史名称。
